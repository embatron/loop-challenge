<?php

/**
 * Implements hook_theme_suggestions_HOOK_alter().
 */
function looptheme_theme_suggestions_node_alter(array &$suggestions, array $variables) {
  if ($variables['elements']['#bundle'] == 'loop_event') {
    $suggestions[] = 'node__' . $variables['elements']['#bundle'];
  }
}